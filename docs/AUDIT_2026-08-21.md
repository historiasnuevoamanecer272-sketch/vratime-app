# VratiMe audit — 2026-08-21

Branch: `codex/vratime-revival`

## Passed

- `npm test`: lint, 187 translation keys across `ru/me/en`, category search, multi-messenger contacts, 7 ordered migration checks, production build and PWA/static checks.
- GitHub Pages build with base `/vratime-app/` and PWA assets.
- `npm audit --omit=dev`: 0 known vulnerabilities.
- `.env` is ignored; only `.env.example` is tracked.
- Public UI at 360, 390, 768 and 1440 px without horizontal overflow; Russian, Montenegrin and English selection persists after reload.
- Open Graph image is 1200×630; PWA icons are 192×192 and 512×512; branded favicon is present.
- Live anonymous Supabase checks confirm 20 translated categories, 5 currently active listings and blocked direct access to profiles, contacts, transactions and reviews. The total original listing/profile counts need the privileged E2E check.

## Fixed during this audit

- Toasts now render on login, loading, profile-error and onboarding screens.
- Added an honest “What are eco points for?” profile section in all three languages.
- Listing quantity is clamped to the database limit of 10,000.
- Deactivating a listing now asks for confirmation.
- Map markers and dialogs have stronger accessible labels/semantics.
- Messenger, Telegram phone, participant-cancellation and live E2E checks cover the latest behavior.
- Replaced the old purple template favicon with the VratiMe teal leaf.

## External/live items still required

- The live project currently allows anonymous execution of `get_my_deals`, but returns an empty array because `auth.uid()` is null. Apply all pending migrations, including `20260821002000_restrict_rpc_execution.sql`, so the call itself returns 401/403.
- Create a Supabase data/Auth/Storage backup before applying migrations.
- `db:check` and `db:test` require the Postgres host/user/password; `db:e2e` additionally requires a temporary service secret. These secrets must not be committed.
- After migration, run `npm run db:public`, `npm run db:check`, `npm run db:test` and `npm run db:e2e` to verify original counts and the full two-user flow.
- Google OAuth remains disabled until Google Cloud, Supabase redirects and the GitHub variable are configured.
- Push and deployment require separate user approval after local acceptance.
